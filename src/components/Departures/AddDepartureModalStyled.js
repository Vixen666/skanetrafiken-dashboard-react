import styled from "styled-components";
import { Button } from "react-bootstrap";

const AddJourneyButton = styled(Button)`
  background-color: rgba(255, 255, 255, 0.3);
  box-shadow: 0 8px 16px 0 rgba(255, 255, 255, 0.2);
  border-radius: 5px;
  transition: 0.3s;
  border: none;
`;

export default {
  AddJourneyButton,
};
